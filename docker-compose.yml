services:
  postgres:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: rhona
      POSTGRES_PASSWORD: rhona
      POSTGRES_DB: rhona
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rhona"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
