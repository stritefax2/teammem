{
  "framework": "vite",
  "buildCommand": "cd ../.. && pnpm run build --filter=@teammem/web",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
