{
  "framework": "vite",
  "buildCommand": "cd ../.. && pnpm run build --filter=@rhona/web",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
